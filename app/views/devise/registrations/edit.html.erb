<div class="row">
  <div class="col card2 m4 s12 offset-m4 center">

    <h5>Edit <%= resource_name.to_s.humanize %></h5>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
      <%= render partial: "shared/devisemes" %>

      <div class="input-field">
        <%= f.text_field :name, :id => "name", :class => "validate", :placeholder => "Name", autofocus: true %>
      </div>

      <div class="input-field">
        <%= f.email_field :email, :id => "email", :placeholder => "Email", :class => "validate" %>
      </div>

      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
        <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
      <% end %>

      <div class="input-field">
        <%= f.password_field :password, :id => "password", :placeholder => "Forgot password", :class => "validate", autocomplete: "off" %>
      </div>

      <div class="input-field">
        <%= f.password_field :password_confirmation, :id => "password_confirmation", :placeholder => "Confirm Password", :class => "validate", autocomplete: "off" %>
      </div>

      <div class="input-field">
        <%= f.password_field :current_password, :id => "current_password", :placeholder => "Inical Password", :class => "validate", autocomplete: "off" %>
      </div>

      <button class="btn waves-effect waves-light" type="submit" name="action">Actualizar</button>
      <%= link_to "Regresar", :back, :class => "btn waves-effect waves-light" %>
    <% end %>

  </div>
</div>
